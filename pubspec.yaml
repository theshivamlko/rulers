name: rulers
description: A Flutter Widget to show Ruler with selected values.Rulers like UI to show measurement or selection within some range.
version: 1.0.5+1
homepage: https://navoki.com
repository: https://github.com/theshivamlko/rulers_flutter_package
issue_tracker: https://github.com/theshivamlko/rulers_flutter_package/issues
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  #
